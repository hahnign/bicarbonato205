---
layout: layouts/base.njk
---
{% from "macros/card.njk" import card %}

<article class="page-section container">
  <div class="detail-card">
    {% if cover %}
      <img src="{{ cover }}" alt="{{ title }}" decoding="async" class="detail-card__cover">
    {% endif %}
    <span class="card__tag card__tag--tema">{{ type or "Tema" }}</span>
    <h1 class="detail-card__title">{{ title }}</h1>
    <p class="card__meta">{{ date | formatDate }}</p>
    <div class="detail-card__desc">{{ content | safe }}</div>

    {% set links = { streamingUrl: streamingUrl, links: links } | collectLinks %}
    {% if links.length %}
      <div class="detail-card__actions">
        {% for link in links %}
          {% set icon = link | platformIcon %}
          <a class="btn btn--sm btn--pill" href="{{ link }}">
            {% if icon %}<img src="{{ icon }}" alt="" class="btn__icon">{% endif %}
            {{ link | platformLabel }} →
          </a>
        {% endfor %}
      </div>
    {% endif %}
  </div>

  {% set prev = collections.tema | siblingItem(page.url, "prev") %}
  {% set next = collections.tema | siblingItem(page.url, "next") %}
  <nav class="detail__siblings">
    {% if prev %}<a href="{{ prev.url }}">← {{ prev.data.title }}</a>{% endif %}
    {% if next %}<a href="{{ next.url }}">{{ next.data.title }} →</a>{% endif %}
  </nav>
</article>

<section class="page-section">
  <h2 class="page-section__heading">También te puede interesar</h2>
  <div class="card-grid">
    {% for item in collections.tema | relatedItems(page.url, 3) %}
      {{ card(item.data.title, item.data.date | formatDate, item.templateContent, "Tema", "card__tag--tema", item.data.cover, item.data | collectLinks) }}
    {% endfor %}
  </div>
</section>
